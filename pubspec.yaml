name: my_project1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you intend to publish your package.

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^2.3.0
  shared_preferences: ^2.0.0  # Зверніть увагу на відступи

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

